=== Book record Plugin ===

Book record is a plugin which is developed with react in the Dashboard (FronEnd) I'm using Tailwind CSS and a Component library of tailwind named Metarial-tailwind. You can check the package.json file about hte packaged we used.

= Features =
1. Create Book record
2. View All record
3. Pagination
4. Search Records
5. RestAPI

= How To Install =
As everything is bundeld and linked, It's ready to Test. You can setup the react enveronment just open the terminal this folder and run npm install. ( Make sure you have installed npm in your machine )

** Note: We didn's use any Caching **


=== Thank you ===
